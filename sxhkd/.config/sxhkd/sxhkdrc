alt + {_,shift} + Tab
    bspc node {next,prev}.window -f
alt + q
    bspc node focused -c
alt + m
    bspc desktop --layout next
alt + l
    systemctl suspend
alt + shift + t
    alacritty
alt + {p,bracketleft,bracketright}
    playerctl {play-pause,previous,next}
