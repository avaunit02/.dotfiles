alt + {_,shift} + Tab
    bspc node {next,prev} -f
alt + q
    bspc node focused -c
alt + shift + q
    bspc quit
alt + m
    bspc desktop --layout next
alt + l
    systemctl suspend
alt + shift + t
    alacritty
alt + b
    firefox
alt + r
    ~/.dotfiles/reset.sh
